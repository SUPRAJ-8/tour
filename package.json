{
  "name": "tour-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "dependencies": {
    "fuse.js": "^7.1.0",
    "quill": "^2.0.3",
    "react-icons": "^5.5.0",
    "react-quill": "^2.0.0"
  }
}
